{% extends "index.html.twig" %}

{% block section %}

    <div class="container p-5">
        <form method="post" action="index.php">
            <fieldset><legend class="pb-3">Choix de la catégorie à modifier</legend>
                <select name="idprojet" class="form-control form-control-sm mb-3">
                    {#  {% for proj in projets %}
<option value="{{cat}}">{{proj.titre}} - {{proj.descriptionproj}} - {{proj.liendemo}} - {{proj.idcontexte}}</option>
                    {% endfor %}#}
                </select>
                <button type="submit" name="cate_modif"  class="btn btn-primary">Valider</button>
            </fieldset>
        </form>
    </div>

    <div class="container p-5">
        <form method="post" action="index.php">
            <fieldset><legend class="pb-3">Choix de la catégorie à supprimer</legend>
                <select name="idprojet" class="form-control form-control-sm mb-3">
                    {#  {% for proj in projets %}
<option value="{{cat}}">{{proj.titre}} - {{proj.descriptionproj}} - {{proj.liendemo}} - {{proj.idcontexte}}</option>
                    {% endfor %}#}
                </select>
                <button type="submit" name="cate_supp"  class="btn btn-primary">Supprimer</button>
            </fieldset>
        </form>
    </div>


    <div class="container p-5">
        <form method="post" action="index.php">
            <fieldset><legend class="pb-3">Ajouter une catégorie</legend>
                <div class="form-group mb-3">
                    <label for="nomcate">Nom de la catégorie</label>
                    <input type="text" class="form-control" id="nomcate" name="nomcate" required placeholder="Entrez le nom d'une catégorie">
                </div>
                <button type="submit" name="ajouter-cate"  class="btn btn-primary">Ajouter</button>
            </fieldset>
        </form>
    </div>





{% endblock %}